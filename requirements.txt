numpy
matplotlib
Pillow

# voxel.py (mesh -> voxel shell); also generate/utils.py's exact-block
# preview renderer (offscreen exposed-face mesh rendering)
open3d

# slice.py --debug (coordinate plotting)
opencv-python

# generate/*.py: optional .schem export for loading into a game save
mcschematic
